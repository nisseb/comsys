function [c1 c2] = single_point_crossover(a, b, cross_prob)

    if (rand < cross_prob)
        cp = randi(length(a));
        
        c1 = [a(1:cp) b(cp+1:
        
    else
        c1 = a;
        c2 = b;
    end
end

